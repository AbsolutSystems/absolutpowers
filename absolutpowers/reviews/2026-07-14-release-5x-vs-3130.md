# Full Review Report — całość 5.x vs v3.13.0 (`ff9cd48`)

> **Zakres:** `ff9cd48` (v3.13.0) → HEAD (v5.1.0) — 154 pliki, +12291/−5859. Cała migracja hybrydowa
> (5.0.0) + feature-discuss scope-fix (5.0.1) + try-learn codebase-scan (5.1.0). Repo bez buildu →
> weryfikacja grep/strukturalna. Brak `rules.md`/`constitution.md` → Faza 3 pominięta.
> Głębokie czytanie zdelegowane do 2 równoległych reviewerów (kod/config + spójność drzewa); ta synteza
> łączy ich ustalenia z walidacją strukturalną orchestratora.

## 1. Semantic Review

### Co się zmieniło (na poziomie architektury):
- **Struktura repo (5.0.0):** dwa mirrory `claude/`+`codex/` → jedno host-agnostyczne drzewo `skills/`. Cienkie manifesty per harness + `.pi/extensions/absolutpowers.ts` + slim SessionStart hook. `AGENTS.md` = symlink do CLAUDE.md.
- **Vendoring (5.0.0):** 7 skilli obry pod `skills/vendored/` (MIT), + forked scripts (review-package/sdd-workspace), + visual-companion.
- **Pipeline domenowy:** grep-AC, terminal-state kontrakty (Faza 3), orchestrated implement (4-status worker + model routing + progress.md ledger).
- **5.0.1:** feature-discuss rozróżnia pytania preferencja-vs-zakres-z-podstawą.
- **5.1.0:** try-learn → codebase-scan; harvest usunięty; archiwizacja → ship.

### Blast Radius:
- Usunięcie `claude/`+`codex/` mirrorów — potwierdzone: `codex/`, `scripts/diff-skills.sh`, `scripts/sync_claude_to_agents.py` nie istnieją. Zero stale ścieżek mirrorów w żywych promptach/manifestach.
- Jedyny realny kod wykonawczy: `.pi/extensions/absolutpowers.ts` (+ vendored companion Node). Zweryfikowany — patrz Faza 2.

### Pytania do autora:
- Konwencja `ship`: ogniwo pipeline czy post-closeout? (patrz Faza 4 / MED — do rozstrzygnięcia).

## 2. Edge Cases

### WYSOKIE RYZYKO: brak.
### ŚREDNIE RYZYKO: brak.

### Zweryfikowane powierzchnie wykonawcze (reviewer kod/config — wszystkie CLEAN):
- `.pi/extensions/absolutpowers.ts` — ścieżki z `import.meta.url` (portable), guard duplikatu injekcji, read w try/catch z null-cache, brak eval/exec, treść wstrzykiwana jako message (nie wykonywana). Bez bugów.
- Hook chain (`hooks.json`/`run-hook.cmd`/`session-start`/`session-context.md`) — matcher poprawny, `${CLAUDE_PLUGIN_ROOT}` (bez hardcode), escaping JSON w poprawnej kolejności, `printf %s` bez format-injection. Emituje poprawny JSON.
- Forked scripts (`review-package`/`sdd-workspace`) — `AP_TASKS_DIR` hard-fail gdy unset, `set -euo pipefail`, walidacja BASE/HEAD przez `git rev-parse --verify`, zero destrukcyjnych operacji.
- Companion (`server.cjs`/`helper.js`/`start-server.sh`) — static-render only (brak eval), telemetria zneutralizowana (`SUPERPOWERS_TELEMETRY_DISABLED=true`), token per-sesja + `timingSafeEqual`, Origin check WS, odrzucanie symlink/traversal, perms 0600.

## 3. Rules Check
Brak pliku `./absolutpowers/rules.md`, pomijam sprawdzanie reguł.

### Pryncypia (constitution)
Brak pliku `./absolutpowers/constitution.md`, pomijam sprawdzanie pryncypiów.

## 4. Garbage Collection

### Do usunięcia: brak (zero martwych importów/TODO/pustych bloków w kodzie).

### Znaleziska (framing/consistency):

**[MED] Niespójne framowanie `ship` — pipeline-step vs closeout.** Dwie grupy powierzchni:
- Grupa A (ship = krok pipeline): `README.md:63` (diagram), `README.md:157` (tabela), `README.md:169` („6 core pipeline skills"), `docs/getting-started.md:51` (kolumna Pipeline), `skills/implement/SKILL.md:643` („review → ship → merge").
- Grupa B (4-skillowy łańcuch, review=domknięcie, ship poza): `hooks/session-context.md:6` (pomija ship), `CLAUDE.md:57/66` („four pipeline skills… review = closure, not a chain link"), `README.md:130`, `skills/review/SKILL.md:313`, `skills/ship/SKILL.md` (brak `## Terminal state`), `docs/getting-started.md:224` (FAQ pomija ship).
- `getting-started.md` przeczy sam sobie (tabela l.51 vs FAQ l.224). Brak zepsutych referencji — czyste framing. **Fix (poniżej):** ujednolicić.

**[LOW] `README.md:169`** — „6 core pipeline skills" myli: karty obejmują też `try-learn-skill` (skill wiedzy, nie pipeline). Reword.

**[LOW ×4, vendored companion — by-design/opcjonalne]:**
- `server.cjs:528` — `cp.exec(BRAINSTORM_OPEN_CMD + …)` przez shell; trusted operator input, default używa `execFile` z argv. By-design.
- `start-server.sh` — brak `command -v node` guardu; fallback bez Node egzekwowany prozą (SKILL.md:49) + wolny generyczny backstop. Opcjonalny fix: wczesny guard.
- `stop-server.sh:113` — `[[ $SESSION_DIR == /tmp/* ]]` przed `rm -rf` łapie `/tmp/../…`; SESSION_DIR z orchestratora (trusted). Opcjonalny fix: `realpath` + re-check.
- `.superpowers/` naming (start-server.sh) — artefakt vendoringu, kosmetyczny.

### Do sprawdzenia (housekeeping, nie defekt):
- Stare artefakty `absolutpowers/feature/*.md` (self-dogfooding sprzed 5.0) referują mirrory `claude/`+`codex/` i harvest (np. `tasks-harvest-docs.md`, `planning-orchestrated-implementation-phases.md`). Nie są częścią shipowanego drzewa (archiwum de facto) → rozważ przeniesienie do `absolutpowers/archives/` by grepy zostały jednoznaczne.

## Podsumowanie
- Krytyczne problemy: 0
- Ryzyka do sprawdzenia: 0 (kod/config CLEAN)
- Śmieci do usunięcia: 0
- Złamane reguły: 0 (brak rules.md); Naruszone pryncypia: 0 (brak constitution.md)
- Framing/consistency: 1 MED (ship framing) + 1 LOW (README mislabel) + 4 LOW vendored (by-design) + housekeeping
- Weryfikacja końcowa: potwierdzona — JSON×5 walid, hook OK, wersje 5.1.0 zsync, 14 workflow skilli + preboot, wszystkie SKILL.md z frontmatter, usunięcia (codex/, skrypty) potwierdzone, vendoring 7/7 z atrybucją MIT, agents ↔ dispatch 1:1, zero dangling harvest/gnhf/ledger/mirror w żywej powierzchni.
- Ogólna ocena: **Solidne, gotowe do pushu.** Zero HIGH/MED w kodzie/bezpieczeństwie; jedyny MED to czysto-dokumentacyjna niespójność framowania `ship` (do ujednolicenia). Migracja strukturalnie kompletna i czysta.
